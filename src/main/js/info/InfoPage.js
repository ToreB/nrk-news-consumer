import React from 'react';
import BuildProperties from "./BuildProperties";

function InfoPage({ buildProperties }) {
    return <BuildProperties buildProperties={buildProperties} />
}

export default InfoPage;